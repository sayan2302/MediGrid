export default function SettingsPage() {
  return (
    <div className="page-grid">
      <section className="pin-card form-card settings-card">
        <header className="section-head">
          <h3>Settings</h3>
          <p>No configurable preferences are available right now.</p>
        </header>
      </section>
    </div>
  );
}
